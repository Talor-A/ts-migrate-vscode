## TS-Migrate for VS Code

this extension wraps airbnb/ts-migrate. It's still in early alpha development.

current features:
- migrate one file at a time using default plugins.

many more features coming soon.